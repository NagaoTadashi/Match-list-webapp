<h1>試合一覧</h1>
<%= link_to 'new' , new_match_path %>
<ul>
  <% @matches.each do | game | %> 
  <li>
    <%= game.Sec %>
    <%= game.Date %>
    <%= game.Venue %>
    <%= game.Oppnent %>
    <%= link_to '[edit]',edit_match_path(game) %>
    <%= link_to '[destroy]',match_path(game),
      data: {turbo_method: 'delete',turbo_confirm: 'Are you sure?'} %>
  </li>
  <% end %>
</ul>
